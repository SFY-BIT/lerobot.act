 conda activate lerobot
conda env export -n lerobot --no-builds > lerobot_env.yaml
pip freeze > lerobot_requirements.txt



conda env create -f lerobot_env.yaml
conda activate lerobot
cd lerobot_piper
pip install -e .

pip install -r lerobot_requirements.txt
